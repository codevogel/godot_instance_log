# Normalize line endings for all files that Git considers text files.
* text=auto eol=lf

# Only include the addons folder when downloading a zip file from GitHub.
/**        export-ignore
/addons    !export-ignore
/addons/** !export-ignore
